import { GopnikPipePipe } from './gopnik-pipe.pipe';

describe('GopnikPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GopnikPipePipe();
    expect(pipe).toBeTruthy();
  });
});
